/**
 * A transition from one state to another.
 * Contains the state before the transition, the event that triggers the transition, and the state after the transition.
 */
export type Transition = [string, string, string];
